<%*
// 1. 获取当前选中的文本
const selection = tp.file.selection();

// 2. 如果没有任何选中内容，就提示一下并停止运行
if (!selection) {
    new Notice("没有选中任何文本");
    return;
}

// 3. 核心步骤：用正则表达式把所有HTML标签（<...>）替换成空字符串
const cleaned = selection.replace(/<[^>]*>/g, '');

// 4. 将清理后的纯文本输出到当前位置
tR += cleaned;
_%>